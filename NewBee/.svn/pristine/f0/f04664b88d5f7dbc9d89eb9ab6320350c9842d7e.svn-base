package blservice.commodityblservice;

import po.PackPO;

public interface CommodityBLService_PackEntrance {
	
	//检查是否存在同名特价包，若存在，返回false。否则，将该特价包添加到特价包类别下
	public boolean addPack(PackPO pack);
	
}
